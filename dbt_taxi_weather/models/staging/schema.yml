version: 2

models:
  - name: stg_trips
    columns:
      - name: pickup_ts
        tests: [not_null]
      - name: dropoff_ts
        tests: [not_null]
  - name: stg_weather
    columns:
      - name: weather_hour_local
        tests: [not_null]